name: Build

on:
  push:
    branches: [main, "1.20.1", "26.2"]
  pull_request:
    branches: [main, "1.20.1", "26.2"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - task: ":common:test"
          - task: ":neoforge:build"
          - task: ":fabric:build"
          - task: ":forge:build"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle
      - name: Build ${{ matrix.task }}
        run: ./gradlew ${{ matrix.task }} --stacktrace
        # AI live tests are env-gated and skipped in CI without DEEPSEEK_API_KEY
